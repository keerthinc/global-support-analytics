## 1. Create a virtual environment (recommended)
    python3 -m venv venv

    Activate it:
    source venv/bin/activate

## 2. pip install -r requirements.txt
    pip install -r requirements.txt

## 3. Create .env file for Snowflake credentials
    


